import { IconCarouselDelete } from "@assets";

interface Props {
  width: number;
  height: number;
}

export function IconCarouselDeleteSVG({ width, height }: Props) {
  return <IconCarouselDelete width={width} height={height} />;
}