import { IconCancelWhite } from "@assets";

interface Props {
  width: number;
  height: number;
}

export function IconCancelWhiteSVG({ width, height }: Props) {
  return <IconCancelWhite width={width} height={height} />;
}